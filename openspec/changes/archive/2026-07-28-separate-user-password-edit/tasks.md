## 1. User edit form

- [x] 1.1 Hide password and confirmation inputs only on the existing user edit form while preserving required inputs on user creation.

## 2. Password-change action

- [x] 2.1 Add a localized header action on the user edit page that opens a password-change modal.
- [x] 2.2 Validate and persist only the new password, then show a success notification.

## 3. Verification

- [x] 3.1 Add focused regression coverage for the password-change action when the existing Filament test setup supports it.
- [x] 3.2 Run PHP syntax checks, relevant tests, OpenSpec validation, and diff checks.

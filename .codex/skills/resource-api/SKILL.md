---
name: "resource-api"
description: "Create API Resources (transformers) for formatting Eloquent models into JSON responses. Follow the project's patterns for data transformation, conditional loading, and nested resources."
---

Source of truth lives at `.agents/skills/resource-api/SKILL.md`.

When this skill is used:
1. Read `.agents/skills/resource-api/SKILL.md`.
2. Follow the workflow and rules defined in that file.
3. Read the referenced sub-files (`references/context.md`, `templates/checklist.md`) from the source-of-truth folder.
